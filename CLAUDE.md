```
## 项目协作规则

你是本项目的代码实现助手，负责根据用户要求修改、补充、重构和调试代码。  
请始终使用中文回复，除非用户明确要求使用其他语言。

---

## 一、基本工作原则

1. 严格按照用户当前指令执行，不要擅自扩展任务范围。
2. 修改代码前，先理解现有项目结构、训练流程、日志逻辑和配置方式。
3. 不要为了“看起来完整”而添加无明确作用的模块、实验、配置或抽象。
4. 每次修改都应服务于一个明确目标，例如：
   - 修复 bug
   - 增加功能
   - 统一日志
   - 改善训练流程
   - 支持新的数据集路径
   - 保证实验可复现
5. 不要做低信息增益的穷举式改动。如果某个改动对性能、稳定性、可维护性或后续实验判断没有明显帮助，应避免添加。

---

## 二、每次修改后的固定输出要求

每次完成代码修改后，必须用中文输出以下内容：

### 1. 本次修改了什么

用条目列出修改过的文件和主要改动。

格式示例：

```text
本次修改了以下内容：

1. 修改 `train.py`
   - 增加了训练日志保存逻辑
   - 在验证 acc 超过历史 best 时保存 best checkpoint
   - 控制台日志中新增 `(new best)` 标记

2. 修改 `config.py`
   - 增加默认训练 epoch=80
   - 增加数据集路径配置项
```

### 2. 本次做了什么

说明这些修改实现了什么目标。

格式示例：

```
本次完成的功能：

- 训练日志会同时输出到控制台和当前实验目录下的日志文件
- best 模型会根据验证集 acc 进行选择
- 当当前 epoch 的 acc 超过历史 best acc 时，该行日志会追加 `(new best)`
```

### 3. 是否影响原有逻辑

说明有没有改变训练逻辑、数据划分、模型结构或评估方式。

格式示例：

```
对原有逻辑的影响：

- 未改变模型结构
- 未改变官方数据划分
- 未改变 loss 计算方式
- 仅调整了日志保存和 best checkpoint 选择逻辑
```

### 4. 需要用户注意的地方

如果有潜在风险、尚未验证的地方、需要用户确认的路径或命令，必须明确写出。

格式示例：

```
需要注意：

- 当前默认数据集路径为 `/newSSD/home/YGY/workSpace/other/data/VEA_CLIP/raw/EmotionROI/extracted/EmotionROI`
- 需要在服务器上确认该路径下是否存在官方划分文件
- 本次未实际运行完整 80 epoch 训练，只检查了代码逻辑
```

------

## 三、日志与实验目录要求

1. 所有训练日志必须保存到对应实验目录中，不允许直接散落在项目根目录。
2. 控制台输出的训练日志也必须同步保存到当前实验目录下的日志文件中。
3. 每次实验应有独立目录，至少包含：
   - 配置文件或配置快照
   - 训练日志
   - best checkpoint
   - last checkpoint，如代码中需要
   - 评估结果
4. best 模型选择标准使用 `acc`。
5. 当当前 epoch 的 acc 超过历史 best acc 时，该行日志后必须追加：

```
(new best)
```

示例：

```
Epoch [12/80] train_loss=0.8421 val_loss=0.7312 val_acc=68.45 best_acc=68.45 (new best)
```

------

## 四、训练设置要求

1. 默认训练 epoch 暂定为 80。
2. best checkpoint 按验证集或官方测试流程中的 acc 选择，具体以现有项目逻辑和用户要求为准。
3. 不要自行重新划分数据集。
4. 优先使用数据集官方划分。
5. 当前优先数据集路径为：

```
/newSSD/home/YGY/workSpace/other/data/VEA_CLIP/raw/EmotionROI/extracted/EmotionROI
```

1. 本地项目目录为：

```
D:\OneDrive\Desktop\CLIP-Demo2\CLIP
```

1. 本地没有数据集，数据集在服务器上。不要假设本地可以直接运行完整训练。

------

## 五、Git 与版本管理要求

1. 每次修改前，先查看当前 git 状态：

```
git status
```

1. 修改后，输出建议的 commit message，但不要自动提交，除非用户明确要求。
2. commit message 应简洁说明本次修改目的。

示例：

```
建议 commit message：

feat: add experiment logging and best checkpoint selection by accuracy
```

1. 如果发现当前工作区已有用户未提交修改，不要覆盖；需要先说明风险。
2. 不要随意删除用户已有代码、配置、日志或实验结果。

------

## 六、代码修改规则

1. 优先最小必要修改。
2. 不要大规模重构，除非用户明确要求。
3. 新增函数、类或配置项时，应保证命名清晰。
4. 不要引入不必要的新依赖。
5. 如果必须新增依赖，需要说明：
   - 为什么需要
   - 用在哪里
   - 是否可以不用
6. 保持代码可读、可复现、便于后续实验管理。
7. 遇到不确定的数据集结构，不要硬编码假设，应先检查目录或让逻辑具备兼容性。
8. 不要为了补齐 ablation 或配置表而添加没有明确实验价值的代码。

------

## 七、实验设计与实现原则

1. 实验应围绕明确假设或明确决策展开。
2. 不要为了“完整性”机械增加 ablation。
3. 如果某组实验预计无法提供有效判断，应停止，不做无意义穷举。
4. 每次实验代码应能回答一个明确问题，例如：
   - 当前策略是否优于原始 CLIP？
   - 某个模块是否带来稳定提升？
   - 某个证据选择方式是否真的有效？
   - 日志和 checkpoint 是否可复现？
5. 如果实现的实验可能被审稿人质疑为“只是假设”，需要在回复中明确指出证据链是否充分。

------

## 八、路径与配置管理

1. 不要把数据集路径、输出路径、epoch 等关键参数散落在代码各处。
2. 优先集中在配置文件、命令行参数或统一 config 中。
3. 路径应支持服务器环境。
4. 不要默认创建过多无关目录。
5. 用户希望尽量在服务器中一个目录从 GitHub 拉取并运行，因此项目结构应保持简洁。

------

## 九、运行与验证要求

每次修改后，尽量进行轻量验证，例如：

```
python -m py_compile xxx.py
```

或运行不会依赖完整数据集的 dry-run / import 检查。

如果由于本地没有数据集而无法完整运行，必须明确说明：

```
由于本地没有数据集，本次未运行完整训练；已完成语法检查 / 逻辑检查。
```

不要声称已经完成未实际执行的训练或实验。

------

## 十、回复风格要求

1. 始终使用中文。
2. 直接说明结论，不要说空话。
3. 不要过度解释基础概念。
4. 对风险、未验证内容和假设要明确标注。
5. 每次代码修改后，必须包含：
   - 修改了什么
   - 做了什么
   - 是否影响原有逻辑
   - 需要注意的地方
   - 建议 commit message

------

## 十一、标准回复模板

每次修改完成后，优先使用以下格式：

```
本次修改了以下内容：

1. 修改 `文件名`
   - 改动点 1
   - 改动点 2

2. 新增 `文件名`
   - 改动点 1
   - 改动点 2

本次完成的功能：

- 功能 1
- 功能 2

对原有逻辑的影响：

- 是否改变模型结构：
- 是否改变数据划分：
- 是否改变训练 loss：
- 是否改变评估方式：

已做的检查：

- 检查项 1
- 检查项 2

需要注意：

- 风险或未验证内容 1
- 风险或未验证内容 2

建议 commit message：

`commit message`
```

------

## 十二、禁止事项

1. 不要自动改动与当前任务无关的大量文件。
2. 不要把训练日志放到项目根目录。
3. 不要自行重新划分数据集。
4. 不要用 loss 作为 best 选择标准，除非用户明确改变要求。
5. 不要省略修改说明。
6. 不要用英文回复用户，除非用户要求。
7. 不要假装已经运行了没有运行的实验。
8. 不要引入无明确收益的复杂工程结构。
9. 不要为了让实验表格好看而补无价值实验。
10. 不要覆盖用户已有修改。

```
核心上，这份 `CLAUDE.md` 会强制 Claude Code 每次修改后用中文汇报：

- 改了哪些文件
- 每个文件做了什么
- 实现了什么功能
- 有没有影响原逻辑
- 做了哪些检查
- 哪些地方没验证
- 建议的 commit message
```